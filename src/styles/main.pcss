:root {
    --font-size: 16px;
    --font: sans-serif;
    --black: #000;
}

* {
    margin: 0;
    padding: 0;
}

body {
    font-size: var(--font-size);
    font-family: var(--font);
    color: var(--black);
}
